-- Recorded on the linked Supabase project as 20260402130000 but the original SQL was not in this repo.
-- If you have the real migration (teammate, dashboard SQL log), replace this file with that SQL.
-- Remote already applied this version; `db push` will not re-run it there. Fresh local resets run this no-op only.
SELECT 1;
