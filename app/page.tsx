/**
 * Placeholder for Next.js; the SPA is the Vite app. API lives under app/api/.
 */
export default function Page() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui" }}>
      <p>API routes only — use the Vite app for the UI.</p>
    </main>
  );
}
